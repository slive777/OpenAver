# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [0.13.13] - 2026-08-14

**官方站已經下架的 FC2 片，現在有第二條路。** 上一版把 FC2 改打官方站之後，還是有片子是**官方自己下架**的——那些片在備份站 javten 上還在。本版新增 `FC2-javten` 來源接那條路。它**只在 Windows 桌面版可用**（站方有 Cloudflare 人機驗證，需要一個真的瀏覽器視窗讓你點一次），而且**不參與自動搜尋**，要在重刮彈窗裡手動選。不碰影片檔、不碰你既有的 NFO 與封面。

### Added
#### 🆕 FC2-javten：官方下架的片的備援來源

- **哪些片用得上**：官方站回 404 的那些。實測本地庫裡官方已下架的三顆，javten 全部都有，拿得到**日文原題**、封面、劇照、標籤、賣家與評分。
- **怎麼用**：在影片的重刮彈窗把來源切到 `FC2-javten`。它**不會自動被用到**——跟 JavLibrary 一樣是手動來源，不進批次搜尋、也不開放給 AI agent。
- **第一次會跳出驗證視窗**：跳出來的是真的 javten 瀏覽器視窗，你點一次通過，程式自動接續查詢並回填結果。通過之後不會每次都問。
- **沒有發售日**：javten 的頁面結構裡就沒有這個欄位，不是抓失敗。其餘六個欄位都有。
- **標籤是日文版**（`ハメ撮り`／`素人` 這種），不是站方機翻的繁中版。
- `fc2`（官方站）這條來源**一行沒動**。兩條各自獨立，其中一條不可用不影響另一條。

### Changed
- **JavLibrary 與 FC2-javten 不再標示 `BETA`。** 那個字在說「這功能還沒做好」，但它們是做好的——只是偶爾需要你完成一次瀏覽器驗證。真正該提醒你的時候（例如在區網伺服器上用不到），那顆膠囊本來就會灰掉並直接告訴你原因，不需要一個常駐標記。

### 已知限制
- **`FC2-javten` 僅限 Windows 桌面版。** 開發模式、區網伺服器、NAS 上它會直接灰化並說明「僅限桌面應用程式」，不會假裝在找、然後回一句「查無此片」。
- **javten 是唯一的備援站。** 已窮舉盤點所有已知替代站，其餘全部不可用。javten 若失守，官方下架的片就真的沒有第二條路。
- **Cloudflare 隨時可能升級。** 目前實測都能通過，但那需要保留人工點擊那條路——這也是本來源只能手動、不能批次的原因。
- 番號要打**完整格式** `FC2-PPV-xxxxxxx`。`FC2PPV-xxxxxxx`（少一個連字號）會被通用番號規則截斷——此為既有限制，非本版新增。

## [0.13.12] - 2026-08-14

**FC2 搜尋回來了。** `fc2` 這條來源原本指向的鏡像站整站上了 Cloudflare 挑戰，一般請求全數 403——也就是說**這條來源已經 100% 壞掉一段時間了**，而畫面上顯示的是「找不到資料」，與「這片真的不存在」逐字相同。本版把它改打 FC2 官方站。不碰影片檔、不碰你既有的 NFO 與封面。

### Fixed
#### 🔍 FC2 番號搜得到了（改打官方站）

- **以前**：搜任何 FC2 番號都回「找不到 FC2-PPV-xxxxxxx 的資料」。原因不是番號錯，是那條來源指向的鏡像站整站啟用了 Cloudflare 互動式挑戰，程式收到 403 之後直接放棄。
- **現在**：改打 FC2 官方站（`adult.contents.fc2.com`），拿得到**日文原題、封面、發售日、標籤、賣家、劇照**。番號的數字部分就是官方站的商品編號，所以是一次連線就取回，不必先搜尋再進詳情頁。
- 封面取的是**原尺寸**那張，不是縮圖；劇照走官方縮圖網域的 1280 寬版本。兩者都沿用既有的圖片放行清單，**沒有為此新增任何白名單**。
- **實測**：本地庫 61 顆 FC2 番號打官方站，**57 顆拿得到、3 顆官方已下架**（另 1 顆是番號解析破洞產生的假番號，不算）。那 3 顆備份站都還有，接備份站是下一版的事（見「已知限制」）。

#### 🖼️ 劇照支援的檔案類型對齊 Jellyfin

- **以前**：`extrafanart/` 資料夾裡只有 `fanart*.jpg` 會被當成劇照，其他媒體管理器產的 `.png`／`.webp`／`.tbn`／或用別的命名的圖都被無視。
- **現在**：判準改成與 Jellyfin 一致——**不看檔名、只看副檔名（png/jpg/jpeg/webp/tbn/gif）與檔案非空**。順帶擋掉 macOS 經 SMB 寫入產生的 `._fanart1.jpg`（那類檔約 4KB、副檔名合法，收進來就是破圖）。
- **新增或刪除劇照現在會被偵測到**：以前重新「產生」只在影片檔或 NFO 的修改時間變了才會重讀那部片，所以你往 `extrafanart/` 丟新圖是不會出現的（**這是既有行為，不限於新支援的檔案類型**）。現在張數變了就會重掃那一片。

#### 🖥️ 側邊欄 logo 在某些 Windows 上顯示成破圖

- 精簡版系統、看圖軟體或註冊表清理工具會污染 Windows 登錄檔裡 `.svg` 的內容類型，而 Python 讀登錄檔且蓋掉內建對照表 → 網站以錯誤的類型送出 `favicon.svg` → 瀏覽器對 `<img>` 載入的 SVG 採嚴格檢查，類型不對就整個不顯示。全站只有 SVG 是嚴格的（JS/CSS 早已強制、封面走另一條路、點陣圖瀏覽器會自己嗅探），所以破的只有這一顆。現在 `.svg` 比照既有機制兩層強制白名單。

### 已知限制
- **官方站已下架的片查不到**（實測一個月新品的下架率 6.6%，老片只會更高）。備份站 `fc-javten` 這條手動來源已完成規劃但**尚未實作**，留待下一支 branch。
- **FC2 番號目前只認完整的 `FC2-PPV-xxxxxxx` 寫法**。官方商品頁上印的「商品ID」逐字是 `FC2 PPV 4943690`（空格分隔），直接複製貼上會查不到；`FC2PPV-4943690`、純數字 `4943690` 同樣不行。這是**既有的番號解析行為，本版一行未動**，只是 FC2 這條來源以前根本不會動、所以看不出來。檔名也一樣（`FC2 PPV 4943690.mp4` 拖進來解析不出番號）。
- **FC2 連不上（被擋、逾時、站方回非 200）時，畫面顯示的仍是「找不到資料」**，與其他七個來源一樣。本版開發途中做過一版「被擋 ≠ 查無此片」的區分（會在畫面與 API 上分流），出貨前整組撤回——缺資料的片在封面牆上本來就是一格破圖、點進去也是空的，那比任何提示都顯眼，而手動補資料的入口從來不受影響。
- **唯讀來源的片**新增劇照後**偵測不到**（唯讀來源走的是另一套產出流程，它只認「這片刮過了沒」，不看磁碟上的檔案）。要更新只能對那部片按「重新刮削」——按「產生」不會有反應。
- 「張數一樣但換了檔案」偵測不到（例如把 5 張全換成另外 5 張）——那要比對每張圖的修改時間，成本不划算。要它更新：隨手多放或移走一張讓張數變一下，或對那部片按「重新刮削」。**不要用「清除快取」**——那會清掉整個資料庫，連你手動調過的封面對焦位置一起沒有。
- 舊的鏡像站實作（`core/scrapers/fc2.py`）保留在原地、測試持續全綠，但**不再被任何搜尋路徑引用**——它是下一支 branch 接手備份站來源時的起點。

### 測試
- 全套 pytest **7030 passed, 1 skipped**（v0.13.11 為 6986，本版新增 44 支）。`npm test` **753**（與 v0.13.11 相同——純後端功能）。`npm run lint`、`node scripts/static_guard_lint.mjs`（1113 條）、`ruff check .`、`lint-imports`、函式規模閘全綠，四條架構閘零新增豁免。
- **8 源金絲雀全 PASS**（含 fc2 打真站台），無一 skip。
- **解析契約全部餵真檔**：官方商品頁 3 顆 ＋ 軟 404 頁 1 顆進版控當 fixture。三個 plan 階段寫錯的解析假設是靠真檔打出來的——① 標題其實沒有被截斷（規劃時假設會截斷）② 劇照的 class 掛在 `<ul>` 不是 `<section>`，照 plan 寫會拿到 **0 張** ③ 發售日那個 class 同頁出現 **3 次**（不是 2 次），照順序取會拿到「對應裝置」。
- **軟 404 用正向標記判定**：不存在的番號官方站回 **HTTP 200 加一張長得很像商品頁的頁面**（兩顆不同的不存在番號，回來的頁面位元組數完全相同）。判準刻意不寫「頁面含『お探しの商品が見つかりませんでした』」——那是日文文案，站方改字就靜默失效，而失效方向是把下架讀成抓到、**寫一筆空資料進 NFO 和資料庫**。改判「`og:url` 存在且等於我們請求的那個商品編號」，同時擋掉「軟 404」與「拿到別片」。
- **真機驗收（活站 ＋ 瀏覽器）**：三顆真番號逐欄與 fixture 相同；封面（145,005 B）與劇照（59,813 B）各自經圖片代理與**寫入路徑**兩條各取回一次、瀏覽器實載 14 張圖零破圖。
- **開發途中做完又整組撤回一個功能**：「被擋 ≠ 查無此片」的全鏈路訊號（型別、聚合層記錄點、四個畫面的文案、API 欄位、5 支 pytest ＋ 2 支前端測試，約 235 行產品碼 ＋ 1,519 行測試）在 PR review 階段被判定為過度設計，出貨前完整撤除。撤除以機械方式自證：每個「只被該功能改過」的檔案，與功能開始前的 commit 逐位元相同（13 檔全過，由獨立 reviewer 復驗），全庫關鍵字掃描零殘留。
- **跨廠牌 review（T2＝Stage 2 一台）**：codex 5.6-terra 三輪。前兩輪針對上述那個後來被撤除的功能（第一輪 3 條 P2 全採納、第二輪 LGTM）；PR review 又提 P1／P2 各一條，追那兩條時重新檢視功能本身，結論是整條撤回——**兩條 finding 連同它們指的機制一起消失**。
- 守衛總量三桶棘輪 **11,742 行**（v0.13.11 為 11,750，−8）。本 branch 對三桶**零改動**。

## [0.13.11] - 2026-08-12

**現在可以看見自己片庫裡有誰了。** 女優牆的 `+` 打開一份「從片庫加入女優」清單：庫裡的人依片數由多到少排好，已經收藏的標實心，沒收藏的一顆一顆按就進牆。這一版只動瀏覽頁，不碰影片檔、NFO 或任何設定。

### Added
#### 👥 從片庫加入女優

- **以前**：把女優牆填起來只有兩條路——影片模式看到順手收，或用工具列那顆 `+` 打開一個塞著文字框的小 dropdown，**要求你已經知道正確全名而且打得出來**（`佐々木あき`、`神宮寺ナオ`）。打錯或打不全就是一句「查無此女優」。也就是說：篩選工具做得再好，填料的漏斗只有那一條。實測兩個真實庫都是**「庫裡有五百個人、牆上只有四十個」**，而「我庫裡到底有誰、各幾片」這份資訊從來沒有出口。
- **現在**：`+` 打開一個置中彈窗，庫內女優**依片數由多到少**排出來，每一列是「片數／名字／愛心」。它同時是**探索**（原來我不知不覺收了她 41 片）與**補完**（那就把她加進最愛牆）。
- **同一個人的不同寫法算同一個人**：別名系統登記過的名字會合併成一列，片數是合併後的總數，不會同一個人出現兩次、各算一半。
- **搜尋比對的是全部資料，不是只有已顯示的那幾列**——打「佐」找得到還沒被捲出來的人。**別名也搜得到**：你記得的是哪個寫法都行。
- **打了全名但庫裡一片都沒有**（例如在外面看到想先收起來）→ 清單底下出現「直接新增『某某』」，走的是原本那條新增路徑。
- **一次可以連按好幾個**：每列各自顯示「排隊中 → 轉圈 → 實心愛心」，**同時最多送兩個請求**（不會一次打爆），**同一個人不管按幾下都只送一次**。某一列失敗只有那一列變回空心可以重按，不影響其他人。
- **AI agent 也讀得到這份清單**（`GET /api/actresses/library`），可以叫它「把庫裡超過 20 片的人都收進最愛」。

#### 🖱️ 清單滑到接近底部自動接著長

- 清單一次顯示 40 位。**以前**要按「展開更多」，**現在**滑到距離底部還有一段（400px，約還剩 7 列）就自動接上下一批，不用停下來點。底部的「已顯示 80／共 311 位」照舊固定在清單外，不會跟著捲走。
- 「展開更多」按鈕連同它的樣式與文案一併退場。

### Changed
#### ➕ `+` 移到搜尋列旁邊

- **以前**`+` 夾在「排序」和「升降序」中間，視覺重量跟「換個排序方向」一樣——但它其實是「牆上還沒有人」時最該按的那顆。
- **現在**它在搜尋列右側、箭頭的左邊，也就是你在「找人」時眼睛所在的位置。**搜尋列是空的、或是搜了但一個都沒找到時它才出現**——「這個人不在我庫裡」正是最想新增的時刻，所以那時候它不會躲起來。打字打到有結果時它收起、清除鈕接上它的位置，按鈕數量不變、版面不會跳動。
- 滑鼠移上去會放大並轉成主題色，按下去微縮，鍵盤 Tab 到它會有一圈輝光。

#### 🗑️ 女優燈箱的刪除鈕換位置

- **以前**它混在照片上那排 hover 才浮現的圓鈕裡——那排放的是「搜尋她的片」「換照片」「對焦」這些常用動作，唯一的破壞性操作擠在裡面，份量不對也容易誤觸。
- **現在**它在下方名字那一行的行末，平常是灰的小圖示、滑上去才轉紅，跟影片燈箱的刪除鈕同一種語言。**正在拖曳調整頭像對焦時它會消失**（維持原本的互斥），**沒有年齡／身高／罩杯資料的女優也照樣有這顆鈕**。

### 已知限制
- 自動展開之後沒有「按一下多 40 位」的按鈕了，純鍵盤操作只能靠捲動帶動；螢幕閱讀器不會被告知清單正在變長。這是單人使用的私人媒體庫，權衡後接受。
- 在面板裡收藏的人，要**關掉面板**才會看到女優牆上多了他們——面板蓋在牆前面，連收 10 個人當下不會有動靜。
- 「打了字 → 清單自動長到 120 位 → 把字清掉」之後，顯示的列數不會退回 40。
- 取消收藏不會刪掉別名表裡那個人的別名列（既有行為）。
- 新增的介面文字本版只有繁體中文；簡中／英文／日文會回退顯示，留待下次 milestone 統一補上。

### 測試
- 全套 pytest **6986 passed, 1 skipped**（v0.13.10 為 6955，本版新增 31 支，全在庫內女優聚合端點與收藏回應）。`npm test` **753**（v0.13.10 為 704）。`node scripts/static_guard_lint.mjs` **1114 條**、`npm run lint`、`ruff check .`、`lint-imports`、函式規模閘全綠，四條架構閘零新增豁免。
- **CDP 真機驗收抓到三個「測試全綠但功能是錯的」**，三個都是規劃階段推理錯、實作忠實照做的結果：
  1. **自動展開的「提前 400px」其實完全沒有生效**。用的是瀏覽器 IntersectionObserver 的 `rootMargin`，但那個參數撐大的是「整個視窗」，而清單自己的裁切發生在更早一步——實測距離底部 500／400／300／200／100／50px **全都不觸發**，只有真的捲到最底才展開。改成用哨兵元素自己的高度當預取帶（並用等量負邊距抵銷版面高度，實測捲動長度 2272→2271px），距底 300px 就會展開。
  2. **名字行的彈性寬度寫法在超長名字時會把兩顆按鈕整組擠到第二行**（分行是用「原始內容寬度」算的，縮放發生在分行之後）。改成與影片燈箱同一種寫法後，51 個字的名字加上英文名，兩顆按鈕仍在同一行。
  3. **一條守衛的掃描範圍開太大，把按鈕搬到區塊外面幾行它照樣是綠的**。用「把按鈕搬出去必須轉紅」逼出上界，範圍由 2800 收到 2170。
- **另有一條「測試看起來在守、其實什麼都沒守」**：「影片模式留著搜尋字、切到女優模式後 `+` 仍要出現」那支測試，因為測試環境的全域旗標永遠是 false，就算實作真的改回去讀那個旗標它也照樣綠——而它正是那條規則唯一的防線。已改成在該測試內把旗標設成真實情境的值，並用兩種回歸寫法各驗一次確認它真的會紅。
- 女優模式的四個真機驗收（面板開關與焦點、逐列收藏、跨模式切換、窄螢幕）都在停用快取的硬重新整理後用真的點擊與鍵盤走過一次；刪除鈕只驗到確認視窗出現為止，沒有真的刪任何資料。
- 守衛總量三桶棘輪 **11,750 行**（v0.13.10 為 11,421，+329）。成長全部在 `static_guard_lint.mjs`：其中 T6 把原本寫在 pytest 裡的前端字串斷言整批遷進 lint（**淨新增守衛 0 條**的搬家），本批三支再各加 3／3／1 條。同期 `test_frontend_lint.py` 淨減 29 行。
- **跨廠牌 reviewer 未跑**（T3 儀式強度：純前端、零後端 diff、零檔案系統寫入）。正確性由每 task 的獨立 review ＋ CDP 真機驗收 ＋ PR review 承擔。

## [0.13.10] - 2026-08-11

**女優牆現在可以用年齡、身高、罩杯篩選，而且條件可以放寬。** 在女優燈箱裡點那些數字，牆上就只留下符合的人；再點一下那枚條件，可以改成「以下」「以上」或自訂一段區間。這一版只動瀏覽頁的女優牆，不碰影片檔、NFO 或任何設定。

### Added
#### 👤 點燈箱裡的數字 → 女優牆長出一枚條件

- **以前**：女優牆只有兩種收斂方式——用名字搜尋（前提是你已經知道要找誰），或按單一維度排序。「身高 165 以下、而且 B 罩杯」這種問題排序永遠湊不出答案：你按身高排完，還是得用眼睛在裡面找罩杯。
- **現在**：打開女優燈箱，第二列的**年齡／身高／罩杯**三格變成可點。點下去燈箱關閉、回到牆上，搜尋列出現一枚條件（例如 `=B罩杯`），牆上只剩符合的人。
- **多枚可以疊**，取交集；也可以同時在搜尋框打名字，兩者一起生效。
- **同一個維度只會有一枚**——再點別人的身高是取代，不是多長一枚。
- 移除方式與影片模式一致三種：按條件上的 ✕、在**空的**女優搜尋框按 Backspace 刪掉最後一枚、按清除鈕清光。用注音／日文輸入法**正在組字**時按 Backspace 只會退你正在打的字。

#### 🎚️ 條件可以放寬

- 點條件本體（不是 ✕）會在搜尋列下方展開一個小浮層：**等於／以下／以上**三顆鈕，**點下去立刻套用**，不必再按確認。
- 底下常駐一列「自訂區間」：填兩個數字按 ✓ 就變成 `150~165cm`。**只填一邊也可以**——`155 ~ 空白` 按 ✓ 就等於直接按「以上」，得到 `≥155cm`；兩條路徑產出的條件完全一樣，不會多長出一種殘缺的寫法。
- **罩杯沒有「自訂區間」那一列**（浮層上根本不會出現它）——罩杯是 A~K 的分級、格數本來就少，`≤B罩杯`／`≥E罩杯` 再配上排序已經夠用。**等於／以下／以上三顆鈕，罩杯一樣可以按。**
- 數字打壞（例如貼進一串瀏覽器解析不出來的東西）時，**只要你按的那顆鈕會用到打壞的那一格，就完全不動作**、浮層也不關——不會把它當成空白、偷偷把條件套成別的值。反過來，按的鈕只用到另一格而那格是好的，照樣正常套用。

### Fixed
- 女優燈箱裡**可點的那三格**，滑鼠移上去的提示以前很弱（只是變亮一點），看不太出來能點。現在改成和影片燈箱一樣的變色＋底線。
- 女優的條件標籤比影片模式胖 7–12px，掛到第三枚就會把搜尋列擠成兩行、工具列變高。現在兩邊一樣寬。

### 已知限制
- 新增的介面文字本版只有繁體中文；簡中／英文／日文會回退顯示，留待下次 milestone 統一補上。

### 測試
- 全套 pytest **6955 passed, 1 skipped**（與 v0.13.9 相同——純前端功能，Python 側只把兩支既有守衛的錨定字串跟著改名遷移）。`npm test` **704**（v0.13.9 為 468，本版新增 236 支）。`npm run lint`、`ruff check .` 全綠，四條架構閘零新增豁免。
- **改名把一支守衛從「真守衛」變成「綠殼」，是靠讀它抓到的**：`_actressCoreMetadata` 改名為 `_actressCoreMetadataParts` 之後，舊字面是新名字的**子字串**，守衛照樣全綠卻已經什麼都沒鎖住。同批的 4 條 `static_guard_lint` 錨定規則一併做等價遷移。
- **CDP 真機驗收 17 條**：三顆鈕即點即套、區間送出、只填一邊等於按「以上／以下」、打壞的數字不被當成空白、浮層外點擊與 Esc 的關閉分支，都是在停用快取的硬重新整理後用真的點擊與鍵盤驗過，幾何結論一律量 content box 與 `getBoundingClientRect()`，不看 `scrollWidth`、不只看 `.value`。
- **一個被真機推翻的設計**：上一階段做的「四種模式鈕 ＋ 種子值 ＋ 超界值夾回」在 owner 真機檢視後整組作廢——多一次「選模式再按確認」的往返，而夾回是把使用者打的數字**靜默改成別的值**。本版改成三顆鈕即點即套 ＋ 常駐區間列，那套介面從未出現在任何已發布版本上。
- 守衛總量三桶棘輪 **11,421 行**（v0.13.9 為 11,416，+5 行**全部是註解**：三處等價遷移的理由）。零新增守衛規則。
- **跨廠牌 reviewer 未跑**（T3 儀式強度）。正確性由每 task 的獨立 review ＋ CDP 真機驗收 ＋ PR review 承擔。

## [0.13.9] - 2026-08-10

**瀏覽頁的篩選從「一次一個關鍵字」變成「可以疊加的條件」。** 在燈箱裡點女優、標籤、片商、導演、系列、廠牌，現在會在搜尋框裡長出一枚可以按掉的小標籤，而不是像以前那樣把你原本打的字整個蓋掉。這一版只動瀏覽頁的篩選，不碰影片檔、NFO 或任何設定。

### Added
#### 🏷️ 點 metadata 變成可以疊加的篩選條件

- **以前**：燈箱裡點一個女優，搜尋框的內容被整段換掉。想同時看「這個女優 ＋ 這個片商」做不到——點第二個就把第一個洗掉了。
- **現在**：每點一次就長一枚標籤，六種都可以（女優／標籤／片商／導演／系列／廠牌）。多枚同時存在時取**交集**：只留下同時符合全部條件的片。你自己打的關鍵字也可以跟標籤並存，一起參與交集。
- **拿掉的方式有三種**：按標籤上的 ✕、在**空的**搜尋框按 Backspace 刪掉最後一枚、或按清除鈕一次清光。用注音／日文輸入法**正在組字**時按 Backspace 只會退你正在打的字，不會誤刪標籤。
- **標籤是精準比對，打字仍是模糊比對**——這兩者刻意不同。點「巨乳」只會出現標籤剛好是「巨乳」的片，不會把「巨乳痴女」一起撈進來；但你自己打「巨乳」還是照舊會撈到兩者。點進來的東西是你在畫面上看到的那一個，多給你別的反而是錯的。
- **別名會自動當成同一個**。同一位女優／同一個標籤的不同寫法（別名系統裡登記過的）算同一個條件，不必知道片子當初存的是哪一種寫法。
- **關掉再打開還在**。條件跟原本的搜尋字一樣會被記住。舊版存下來的瀏覽狀態讀得進來，不會因為格式變了就整組消失或讓頁面開不起來。
- 底部狀態列會顯示「**符合 N 個條件的 M 部**」。搜尋不到東西時的原本提示照舊。

#### 👤 女優資料卡只在「單一女優」時出現

- 點女優標籤時，上方仍會出現那位女優的資料卡（照片、片數）。但一旦你再疊上第二個條件，資料卡就會收起來——此時畫面上的片已經不只是「這位女優的全部作品」了，繼續掛著那張卡會讓人誤以為她只有這幾部。

### 已知限制

- **手機直向、又遇到特別長的系列名時，標籤會被截斷成一行**（不會撐破工具列，但看不到完整名稱；長按／滑鼠停留可看到完整值）。已列為後續改善。
- 標籤本身的四種語言介面文字，本版只有繁體中文；其餘三語會回退顯示，留待下次 milestone 統一補上。

### 測試
- 全套 pytest **6955 passed, 1 skipped**（與 v0.13.8 相同——本版是純前端功能，未新增 Python 測試，只把兩支既有守衛遷移到新的 API）。`npm test` **468**（v0.13.8 為 330，本版新增 138 支，分十個檔對應十個 task）。`npm run lint`、`ruff check .`、`lint-imports`、函式規模閘全綠，四條架構閘零新增豁免。
- **十個 task 每一個都做了 mutation 自驗**（T3 儀式強度其實不要求，但這支的測試幾乎全是「讀原始碼字面」型，不自驗就分不出真守衛與綠殼）。過程中靠自驗抓到的問題包括：一支新守衛被實作者自己的註解觸發（改成比對函式定義形狀 ＋ 剝註解，而不是放寬守衛）、一條 `anyOf` 規則的紅燈在 OR 語意下根本不可能出現（修的是任務卡不是守衛）。
- **CDP 真機驗收**：pill 的新增／移除／疊加、Backspace、清除鈕、女優資料卡收斂、狀態列文案，都在停用快取的硬重新整理後用真的點擊驗過一次——這支有數個缺陷是「單元測試全綠但功能是壞的」，包括工具列在 360px 被切（`scrollWidth <= innerWidth` 照樣通過，因為它是被裁掉不是被捲動）與狀態列渲染出 `[showcase.status.pill_prefix]` 字面（後者查明是開發伺服器的翻譯快取沒有 mtime 失效，重啟即消，不是產品缺陷）。
- 守衛總量三桶棘輪 **11,416 行，與 v0.13.8 完全相同**（本版對 `static_guard_lint.mjs` 只做既有規則的等價遷移，行數不變）。順帶更正：`pre-merge.md` 記載的 v0.13.8 基準 11,412 與實測差 4 行，實際值為 11,416。
- **跨廠牌 reviewer 未跑**（T3 儀式強度）。正確性由每 task 的獨立 review ＋ 我方 mutation 自驗 ＋ owner 靜態 code review 承擔。

## [0.13.8] - 2026-08-09

**兩件事：AI agent 現在有自己的一把鑰匙；你的 API 金鑰不再被送進瀏覽器。** 前者只有在你開了上一版的「需要密碼才能連線」時才存在；後者不管你有沒有用區網模式都生效，但你唯一會注意到的變化是設定頁的金鑰欄位變成圓點加後四碼。

### Added
#### 🤖 給 AI agent 的連線鑰匙（需先開啟密碼保護）

- **在哪裡拿**：Help 頁多一塊「AI agent 連線」——連線網址、一組 token（眼睛可看、一鍵複製）、以及一行**可以直接貼進終端機執行**的 `curl` 範例。給 agent 的東西就是「網址 ＋ token」兩樣，其餘它讀 `/api/capabilities` 自己會發現。
- **token 只在跑 OpenAver 的那台電腦上看得到。** 從手機或平板連進來的人，即使已經輸過密碼、Help 頁也不會有這一塊——看不到就沒辦法把它抄走。
- **沒開密碼保護時，Help 頁與上一版逐位元組相同**：不顯示區塊、也不顯示「請先開啟密碼保護」之類的提示。沒開密碼就沒有 token 這個東西，一個給不出東西的區塊只是噪音。
- **怎麼作廢**：**改密碼（含重新存一次同一組密碼）就會換掉 token**，同時所有裝置都要重新輸入密碼。**沒有第二顆「重新產生」按鈕**——那顆鈕在出貨前被拿掉了：同一件事有兩條路徑，多出來的那條只換到「換 token 但不用重新輸密碼」這個幾乎不會發生的需求。
- **拿錯 token 的 agent 會收到一個看得懂的錯誤**（401 ＋ 一句「憑證無效」），不是那張偽裝頁。人看到偽裝頁會走開，程式看到偽裝頁只會卡住。

#### 🗣️ `/api/capabilities` 對 agent 講真話

- 開啟密碼保護後，這支端點以前會**說謊**：它自稱「不需要認證」，並附上 40 個不帶認證的 `curl` 範例。agent 照著打會全部被擋，然後回頭跟你說「你這台好像壞了」。
- 現在認證欄位寫明要帶什麼 header，**40 個範例全部帶著它**，網址也換成你的區網位址——複製貼上就能跑。

### Changed
#### 🔒 API 金鑰不再送進瀏覽器

- **以前**：每次打開設定頁，Gemini／OpenAI 的 API 金鑰與 metatube token 會**完整**送到瀏覽器。桌面版時代這只是「就你自己」；區網模式一開，任何通過密碼的家庭裝置都讀得到。
- **現在**：這三個欄位只回**後四碼**（`••••93rY`）。設定頁看起來幾乎沒變（那些欄位本來就顯示成圓點），後四碼是給你認「這是哪一把」用的。
- **你的使用流程完全不變**：
  - 貼上一把新金鑰，**可以直接按測試連線**，不必先儲存（這點刻意維持原樣）。
  - **不動金鑰直接按儲存**，或在**掃描頁**加一個資料夾按儲存——金鑰都不會被圓點覆蓋掉。（掃描頁那條路是重點：它的儲存流程與翻譯設定毫無關係，卻同樣會把整包設定寫回去。）
  - metatube 按「連線」**不必重新輸入 token**。
- **刻意不遮的兩個欄位**：metatube 網址與 proxy 網址。它們是「連線目標」而不是不透明的祕密，遮了會讓連線功能整個壞掉。代價寫在下面的已知限制。
- `config.json` 首次建立時的檔案權限收緊為 `0600`（以前從預設檔複製過來會是 `0644`）。**這一格不是漏洞**——那一刻的檔案內容不可能含有你的金鑰，收緊是為了讓「這個檔案永遠只有你讀得到」變成真的成立，而不是碰巧成立。

### Known Issues
- **agent 不能用瀏覽器那種 `EventSource` 訂閱進度事件**（掃描進度那類）。把 token 放進網址是能繞過，但那會讓憑證進到伺服器紀錄、瀏覽器歷史與 Referer，是實質的安全退化，所以不做。agent 改用 `curl`／`httpx` 讀同一條串流是正常的。
- **metatube 網址／proxy 網址裡如果內嵌了帳號密碼**（`http://帳號:密碼@主機`，通常是前面擋了反向代理才會這樣設），那串帳密仍然會出現在設定頁與設定 API 的回應裡。它們與上面三個欄位的差別是「值本身就是連線目標」，套不上同一套遮罩。
- **換金鑰時請先全選再貼上。** 如果你把游標移到欄位最後面直接貼，存進去的會是「圓點 ＋ 新金鑰」那一整串，之後翻譯會全部失敗，而欄位又變回圓點、看不出哪裡錯。重新全選貼一次即可修好。
- **完全封鎖 cookie 的瀏覽器**（例如 iOS 無痕模式）輸對密碼也進不去，而且畫面看不出原因（0.13.7 已知限制，本版不變）。

### 測試
- 全套 pytest **6955 passed, 1 skipped**（v0.13.7 為 6854；本版新增 101 支）。`npm run lint`、`npm test`（**330**，v0.13.7 為 322）、`ruff check .`、`lint-imports`、函式規模閘全綠，四條架構閘零新增豁免。**來源金絲雀本版未跑**（T2 儀式強度：未動任何 scraper）。
- **真機驗收**（不是只有測試綠）：在區網位址上實測三態——不帶憑證拿到偽裝頁、亂 token 拿到 401 JSON、真 token 讀得到 capabilities 並**照著範例完成一次真的寫入**；同一支寫入端點不帶 token 時確認**資料沒有被寫進去**。另驗重開程式後 token 不換、已通行裝置不用重新輸入，以及改密碼（存同一組）當場撤掉所有票並換新 token。金鑰側則以真的 Gemini 金鑰與活的 metatube 實測：設定頁原封不動存回去、以及不重輸 token 直接按連線（30 個 provider 全部連上），磁碟上的真值都一字未變。
- 守衛總量三桶棘輪 **11,412 行**（上一版 11,372，+23）。成長全在票表的「單一所有者」規則（core／web／windows 各一條）：破了的後果是別的模組繞過鎖直接寫票表，於是「改密碼會踢掉所有裝置」這個承諾靜默失效——使用者以為踢掉了，實際還連得進來。
- **API 金鑰那三個 commit 零新增守衛**：可證偽性由行為測試承擔（六個入口逐一拔掉，各有一支單獨轉紅的測試）。

## [0.13.7] - 2026-08-09

**區網模式現在可以設密碼了。** 開啟「需要密碼才能連線」之後，同一個 Wi-Fi 底下的其他裝置要輸入 4 位密碼才打得開 OpenAver。沒在用區網模式的話，這一版對你沒有任何可見變化。

### Added
#### 🔑 區網連線密碼保護

- **在哪裡開**：設定頁上方、伺服器模式膠囊旁邊的「需要密碼才能連線」。密碼是 **4 位英文或數字**（區分大小寫）。
- **別人會看到什麼**：不是登入畫面。**是一頁看起來像「這個網址沒東西」的空白頁**——沒有標題、沒有說明、沒有任何字，只有一個小框。這是刻意的：登入畫面等於告訴掃到你 IP 的人「這裡有東西，而且值得保護」。輸對了就直接進站，輸錯了畫面完全不變。**「還沒輸」「輸錯了」「這站真的沒做好」三種狀態長得一模一樣。**
- **輸過一次就不用再輸**：那台裝置會被永久記住，不會每次開都問。
- **你自己這台電腦永遠不用輸**——密碼只擋從網路上連進來的裝置。
- **改密碼或關掉保護，所有裝置一律要重新輸入。** 這是刻意的：你會想改密碼，通常正是因為想把某台裝置踢掉。
- **密碼只能在跑 OpenAver 的那台電腦上改**，從手機或其他裝置連進來的人看不到也改不了。
- **連錯會愈鎖愈久**：連續錯 5 次鎖 1 分鐘，再錯逐級拉長，最多鎖 1 小時。這個鎖是**整體的、不分裝置**——4 位密碼真正的保護力來自這個，不是來自長度。在主機上重設一次密碼可以立刻解除鎖定。

#### ⚠️ 公網暴露警告

- **只抓一種情況**：這台電腦自己在網路上的位址（對外連線走的那張網卡）就是一個**全球可路由**的公開位址——例如直接接數據機、或把 OpenAver 跑在有公網 IP 的 VPS 上。這時設定頁會出現警告：4 位密碼是為「家裡的 Wi-Fi」設計的，一旦暴露到公網就等於沒有。
- **抓不到的情況：路由器上的 port forward。** 你在路由器設了轉發、把 OpenAver 開到外網，這台電腦看到的仍然是 `192.168.x` 這種家用位址，程式**從機器內部原理上就無從得知**。所以：**沒跳警告不等於沒暴露**——要對外開放請自己確認，並改用比 4 位密碼更強的保護。
- 偵測刻意做得很窄：**判斷不出來的時候一律不出聲**。誤報一次之後你就再也不會看這個警告了，那反而更糟。
- 判準用「是不是全球可路由位址」而不是「是不是私有位址」。兩者中間夾著一批特殊網段，其中最常見的是電信業者的 CGNAT（`100.64.x`）——**把筆電接到手機熱點或 4G/5G 路由器時本機拿到的就可能是這種位址**，用舊判準會跳出一則說你暴露在網際網路上的警告，而你並沒有。

#### 🔒 兩道不會被看到的補強

- **別的網站不能借用你的通行證。** 通行證 cookie 用的是 `Lax` 模式（這是刻意的：`Strict` 會讓你用 LINE 傳給家人的連結在第一次點開時失效，而他們看到的會是那張空白偽裝頁、完全不知道發生什麼事）。代價是瀏覽器在「從別的網站導覽過來」時仍會帶上通行證，而閘門後有幾支**用 GET 就會寫檔**的端點（補齊 Jellyfin 圖片、補完 NFO、重建媒體庫）。現在跨站打向 `/api/` 的請求一律當作沒有通行證——分享連結指向的一定是頁面而不是 API，所以分享情境完全不受影響。
- **惡意網頁不能靠亂打把你全家鎖在門外。** 密碼輸入端點原本會把任何格式的請求都算成一次嘗試，於是一個惡意網頁只要連打五次就能觸發全域鎖定（它不需要知道密碼）。現在只有真正的 JSON 請求才計次。

### Known Issues
- **完全封鎖 cookie 的瀏覽器（例如 iOS 無痕模式）輸對密碼也進不去，而且畫面看不出原因。** 「記住這台裝置」靠的就是 cookie。要讓它看得出原因，就得回一個能分辨的畫面——那正好推翻上面整個偽裝設計。改用一般瀏覽視窗即可。

### 測試
- 全套 pytest **6854 passed, 1 skipped**（v0.13.6 為 6596；本版新增 258 支）。`npm run lint`、`npm test`（**322**，v0.13.6 為 318）、`ruff check .`、`lint-imports`、函式規模閘全綠，四條架構閘零新增豁免。來源金絲雀本版未跑（T2 儀式強度：未動任何 scraper）。
- **每一支新守衛都做過 mutation 自驗**，其中三次是靠 mutation 才發現守衛是假綠的：撤銷票只在快取層被證明（把 DB 那行刪掉，63 支測試仍全綠——真實後果是「被踢掉的裝置重開程式後又能進來」）；併發屏障測試拿掉鎖之後仍然綠；「不得比對舊密碼」那支守衛被我用它沒見過的寫法打穿兩次，第二次觸發 `pre-merge.md` 步驟 0.1 的停損規則，改成「新密碼不得出現在任何比較式裡」重寫，而不是繼續往辨識清單裡加來源。
- 最後一個 commit 順帶修掉一個既有 bug：儲存鈕的條件原本寫死 4 位數字，打英文會得到一顆永遠按不下去的灰按鈕，而且沒有任何說明（那句錯誤提示只在伺服器回應時才出現，但按鈕按不下去所以永遠跑不到）。pre-merge 的 Codex 二審抓到**同一個病的第二個實例**——中文輸入法全形模式打出的密碼，前端只認 ASCII、後端卻會折疊，於是儲存鈕同樣不亮同樣不說明；改成在輸入當下就折疊，欄位顯示的即是實際存進去的值。
- 守衛總量三桶棘輪 **11,372 行**（上一版 11,291，+81）。成長全在 `static_guard_lint.mjs`：偽裝頁那條由鎖 `inputmode="numeric"` 換成鎖 `autocapitalize="off"`（換標的非新增），另新增設定頁 PIN 欄的 NFKC 與 autocapitalize 兩條——它們守的是「使用者設出一組自己在別台裝置打不出來的密碼」，破了的後果是進不去自己的服務且看不出原因，不是外觀問題。


## 0.13.x 早期 (v0.13.0 ~ v0.13.6, 2026-08-01 ~ 2026-08-07)

- metatube「換來源」預覽破圖修復（預覽圖改走你自己那台 metatube，絕不寫進封面檔）＋ metatube 帳密三個外流出口封閉＋圖片來源放行政策收斂為單一宣告式清單（v0.13.6）
- **v0.13.0**：唯讀來源單片刮削與批次補料的重複產出邏輯收斂成單一入口（`core/readonly_producer.py`），純內部整併、零行為變更，對使用者無感。
- **v0.13.1**：安全修補 + 內部治理——影像處理元件（Pillow）與檔案上傳解析元件（python-multipart）升級，一次消除 17 個已知 CVE；整理與唯讀庫產出加上「路徑不得跳出指定資料夾」的最終防線；「檢查更新」按鈕補上三道防護，避免惡意網頁背景觸發；開發模式啟動命令改回只綁本機（一般使用者用的桌面版不受影響）。
- **v0.13.2**：外部媒體管理器設為「關閉」時，唯讀來源不再多產出兩張沒人看的 `-poster`／`-fanart` 圖（省空間，畫面與播放不變）；修好 HEYZO 站方改版造成的搜尋整個查無問題，並補回女優名、發售日、系列名三個欄位。
- **v0.13.3**：修好「補齊 Jellyfin 圖片」會把**只有 `-poster` 的片**封面原圖就地裁小且不可還原（含 hardlink／symlink 同檔別名）；修好封面被記成 `-fanart` 的片被誤判成缺 NFO；封面「該存在哪個檔名」的 6 處各自推導收斂成單一決策點（純代換、零行為變更，為下一版的封面命名翻面鋪路）。
- **v0.13.4**：接 Jellyfin／Emby／Kodi 時封面牆的橫式圖塞進直式格子——根因是「多產了一張同名封面」被那些軟體優先讀取；媒體伺服器模式改成不產同名封面、`-fanart` 升格為正典位置，新片封面牆立刻正確且每片省一份圖（**舊片不自動修好**，唯一修法是自行刪掉同名封面，理由是不動使用者已調好的對焦座標）；另修唯讀來源「輸出資料夾設在來源資料夾裡」的三條同檔覆寫 bug（封面記空白／自帶直式海報被機器裁的蓋掉／自帶兩張圖互相蓋）。
- **v0.13.5**：片庫裡有其他工具（Jellyfin／Emby／別的刮削器）留下的 `.nfo` 時，裡面明明有的演員／片商／發行日／分類標籤會被判成缺漏、去網路刮一份回來覆寫掉——四種讀不到的寫法全部修好；另修「欄位有標籤沒內容被當成已有資料」「全空白的 `<release>`／`<num>` 吃掉後面合法的值」「補完寫完檔案卻整片報失敗」「同一位女優兩個下載請求互相覆蓋」等五條靜默 bug；NFO 解析／sidecar 時間讀取／原子寫三塊重複實作收斂為單一所有權。

測試數 5655 → 6332。

## 0.12.x 系列 (v0.12.0 ~ v0.12.10, 2026-07-14 ~ 2026-07-24)

- **焦點裁切上線與收尾**（v0.12.0～v0.12.1、v0.12.3，feature/98/99/101）：無碼封面（FC2／素人／uncensored）刮削或掃描時自動偵測人臉、以臉為中心裁切封面與燈箱大圖，有碼片維持原樣零成本；燈箱補上「自動對焦→左右拖曳微調→存檔」正式互動、唯讀來源也納入自動對焦、牆上小格裁切改與燈箱大圖同精準度；後續再擴及 Jellyfin/Emby/Kodi 媒體伺服器海報，對焦等待動畫改視覺連續交棒，影片對焦鈕改為只在窄螢幕／手機才出現。
- **女優照片管理更完整**（v0.12.2、v0.12.4，feature/100/102）：女優頭像可自行上傳照片、也能像影片一樣手動對焦裁切；修好改過名（有別名）的女優在照片挑選視窗選本機候選必失敗、雲端重抓永遠查不到圖的問題，重抓改為自動輪流用她的每個名字查詢；順手讓搜尋詳情、燈箱、劇照瀏覽都能用滑鼠滾輪切換上一片／下一片。
- **技術債清償**（v0.12.5，feature/103）：約 130 行硬編碼中文文字收進語言檔；拖曳檔案與貼上檔名的番號辨識規則統一為同一套後端規則（不再兩套邏輯漂移）、辨識失敗改明確提示；清除死碼、logger 與中文硬編碼機械 lint 守衛正式上線。
- **唯讀來源（雲端／NAS 分享盤）功能大幅強化**（v0.12.6、v0.12.7，feature/104/105）：已經整理好的唯讀庫（旁邊有 `.nfo`／封面）改為直接就地讀用、不再無腦重刮，唯讀片的放大鏡補料／齒輪重刮／補劇照三顆鈕全面解禁（產物仍一律落獨立輸出夾、來源零寫入）；並把散在四處、常常改一處漏改一處的「記帳與回報」邏輯收斂成一份共用實作，順手修好唯讀破圖、原文標題被清空等一批連帶 bug。
- **整理前手動修正 + 桌面體驗收尾**（v0.12.8～v0.12.10，feature/106/107/108）：拖檔進搜尋頁後可在整理前直接改演員名單、幫無碼片補發售日；桌面版加上開機自動檢查更新（可關）、修好深色主題幾顆看不清的按鈕；手機瀏覽封面吃滿寬度、觸控裝置封面不再擠一排操作圖示（改進燈箱操作）、女優牆卡片大小對齊影片牆不再忽大忽小。

測試數 5233 → 5640（0.12.0 全文未附測試段落，範圍取最早可查的 0.12.1 起；前一系列 0.11.12 收尾值為 5030）。

## 0.11.x 系列 (v0.11.0 ~ v0.11.12, 2026-06-27 ~ 2026-07-12)

- **v0.11.0**：JavBus 過度泛用清償 + exact 番號搜尋改優先序 cascade（feature/85）——直接搜番號改為依你拖曳的來源優先順序逐一查詢（cascade，命中即回），JavBus 不再無視優先序搶先短路；DMM proxy 透傳修復、前綴搜尋 `type` 參數修正；拔除已死的 JavBus variant（同番號多版本）探查死碼。
- **v0.11.1**：JavLibrary 同番號多版本手動切換（feature/86）——搜尋框直搜／燈箱換來源／結果卡替換來源三入口皆可看封面手動挑撞號版本（游標預設停最新發行日），桌面 standalone 限定（需 CF transport）。
- **v0.11.2**：`core/database.py` 模組化拆分（feature/87）——2,152 行單檔拆成 `core/database/` 套件（六個領域子模組 + 永久 re-export facade），消除 `AliasRepository`／`TagAliasRepository` 鏡像重複碼（共用泛型基類），零行為／API／schema 變更。
- **v0.11.3**：唯讀來源生成本地媒體庫「off 風味」首發（feature/88）——scanner 來源可勾「唯讀」＋設輸出夾，來源零寫入下生成每片一資料夾的本地庫（NFO + 封面 + 劇照）並直接寫進 DB，供 OpenAver 自身瀏覽／串流播放雲端原檔；給 Emby/Jellyfin/Kodi 的 media-server（`.strm`）風味延後至下一版（feature/89）。

- **v0.11.4**：唯讀產生庫「地基」+ 掃描頁「試過」記憶 + 來源刪檔清死卡（feature/89）——生成片記住輸出夾（`videos.output_dir`）重刮原地更新不長重複夾、off 風味固定輸出夾免設定；試過／已生成的片不再被「缺資料」嘮叨、刮不到只試一次；唯讀網盤掉線明確警告不誤報成功；來源刪檔後 DB-row-only 清死卡（零檔案刪除）。
- **v0.11.5**：唯讀來源生成媒體伺服器庫「.strm 風味」+ 跨機器路徑映射 + 唯讀寫入全面封鎖（feature/90）——唯讀來源可產出 `.strm` 捷徑檔給 Emby/Jellyfin/Kodi 掃描播放，跨機器「播放端路徑替換」規則讓 `.strm` 內路徑翻成播放端看得懂的形式、改規則一鍵同步改寫既有 `.strm`；同時把唯讀來源「零寫入」補到滴水不漏（勾唯讀破壞性確認、四個寫入入口全面停用、切模式清舊媒體卡、產生中斷乾淨收尾）。
- **v0.11.6**：跨機器路徑映射（WSL2+UNC）讀寫全棧收斂 + DB-key 命名空間守衛（feature/91）——純 correctness 重構：修好「讀取端忘了在碰磁碟前把映射路徑反解回本機路徑」導致縮圖／封面／串流跨機器讀不到的一類 silent bug，以及「已反解路徑又被裸餵回 DB key」導致重刮掉使用者標籤、女優照 403 的另一類；兩支 AST 結構守衛擋死回歸。

- **v0.11.7**：搜尋頁體驗優化（feature/92）——搜尋列右側控制項不再擠壓變形、整理入口常駐可見更好找、整理入庫「飛入側邊欄」動畫加強為三段節奏，並抽成可跨頁複用的共用元件；順手修好「重新整理已有結果頁面時出現兩組 ✕」的 latent bug。
- **v0.11.8**：各刮削來源評分/簡介補進 NFO（feature/93，NFO-only）——七源評分、六源簡介寫進 `<rating>`/`<plot>`，純服務媒體伺服器（Jellyfin/Emby/Kodi）用戶，OpenAver 自身介面不顯示；順手美化燈箱中繼資訊視覺層次。
- **v0.11.9**：掃描頁「補資料」升級成逐片進度卡 + 命中封面飛入圖書館（feature/94）——補資料時逐片可視進度（搜尋中／命中／查無／失敗／唯讀跳過），真封面命中飛入側邊欄「瀏覽」入口。
- **v0.11.10**：設定頁命名區膠囊化 + 列表生成兩層 IA 重排（feature/95）——檔案命名格式從手打字串改成「變數膠囊 + 字面文字」視覺化編輯器、資料夾層級改動態清單（硬上限 3 層）；列表生成設定拆成日常常用（常駐）+ 離線 HTML 匯出（摺疊進階）。
- **v0.11.11**：前端靜態守衛 pytest → lint 全面遷移（feature/96，test-deflation）——純內部工程里程碑（零產品碼、對使用者隱形）：把 `test_frontend_lint.py` 裡「讀原始碼做字串/結構斷言」的守衛搬回工具層（新增 `i18n_lint`／`static_guard_lint`／`css-guard` 三支 `.mjs` + eslint `SEL_*` 家族），`test_frontend_lint.py` 16,749→5,041 行（−70%）、36 個真 contract class relocate 進 `frontend_contracts/`；north-star＝「能用 lint 機械處理的不進 pytest、不耗 Codex 審」。
- **v0.11.12**：javdb 在 released 版復活（feature/97）——打包瘦身刪除所有 `.dist-info` 導致 curl_cffi import-time 拋 `PackageNotFoundError`、被靜默吞掉，released Windows/macOS 版 javdb 從第一天就零 HTTP 請求；不再剝除 dist-info 即修復，並補打包產物 runtime 驗證守衛（`verify_artifact_imports.py` + dist-info 靜態檢查 + CI verify job）堵死 dev-only 測試盲區；本版另 bundle 兩個後續修復：非 ASCII 安裝路徑 `curl error 77`（CAINFO 改用 ANSI code page 編碼）+ 番號 7 字母前綴拖檔截斷修復。

測試數 4735 → 5523（v0.11.10）→ 4985（v0.11.11 test-deflation：守衛遷 lint 層等價承接，−538 非覆蓋損失）→ 5030（v0.11.12，+45：打包產物驗證守衛 + javdb CAINFO + 番號 cap 對齊回歸鎖）。

## 0.10.x 系列 (v0.10.0 ~ v0.10.11, 2026-06-18 ~ 2026-06-24)

- **來源穩定性 + 測試硬化**（v0.10.0，feature/73）：8 源真實番號健康金絲雀 smoke（三態 quorum 判讀）+ avsox 復活（站方轉 SPA 後改打背後 JSON API）+ Tokyo Hot 單字母無碼番號查無修復 + 覆蓋率地板 84%（`cov-floor`）+ 五項高風險模組（含會改寫用戶 NFO 的 `nfo_updater`）離線單元測試債清償。
- **前端呈現與發現性優化**（v0.10.1～v0.10.2，feature/74/75）：進階搜尋畢業為永久常駐核心，隱形長壓手勢全面移除、改「來源膠囊」處處可見觸發挑源／換源；搜尋詳情資訊密度重排、封面正面裁切共用規則、行動裝置基礎相容（scroll trap／星空門檻／觸控 overlay）補齊。
- **開發工具鏈硬化 + 前端離線可靠性**（v0.10.5～v0.10.6，feature/78/79）：`ruff` + eslint/stylelint 進 CI 擋 PR（先前純本地、沒人本地跑就漏）；GSAP/Alpine/圖示字型改本機載入斷網仍可用、前端錯誤自動記錄；`requirements.txt` 精確鎖版並把 Starlette 鎖至最新修補版清除已知 CVE。
- **MPA 跨頁轉場 + Fluent 材質統一**（v0.10.3～v0.10.4，feature/76/77）：純 CSS View Transitions 讓 sidebar 切頁白屏閃爍改平滑淡換（Showcase 因常駐動畫維持硬切）；全站材質收斂成單一 token 系統的 6 角色（Mica canvas／Glass shell／panel／caption／overlay／Media frame）+ 浮動圓角玻璃 chrome 擴及 search／settings／scanner。
- **LAN 伺服器模式 + 手機體驗完整化**（v0.10.7～v0.10.11，feature/80/81/82/83/84）：一鍵把桌面 App 開放給同區網手機／平板瀏覽（dual-listener + 區網存取閘門）；手機加主畫面圖示、封面／燈箱左右滑換片、窄螢幕破版修補；Windows 系統匣關閉行為（最小化背景執行）；燈箱封面比例自適應不留白 + 行動星形爆射相似探索面板；Windows 雙擊安裝捷徑 + Help 頁一鍵更新按鈕。

測試數 4089 → 4735。

---

## 0.9.x 系列 (v0.9.0 ~ v0.9.11, 2026-05-29 ~ 2026-06-13)

- Scraper Federation + metatube HTTP 聯邦（30 provider Parts Bin + promote/demote + 無碼 staged + SSRF 驗證）、Settings 分區 IA + 資料驅動來源 schema + 進階重刮彈窗
- Active Row 拖曳順序成搜尋路由唯一真理（拔除 primary_source）；async-offload 慢 I/O 移出 event loop（NAS stat / sqlite / config / 同步 HTTP）+ config 鎖/原子寫 + AST 守衛；封面三態（skeleton/shimmer/破圖）+ Showcase console 清零
- 新增來源：JavLibrary（BETA，桌面借 PyWebView 過 Cloudflare）+ avsox 復活（轉 JSON API）+ Tokyo Hot 單字母番號修復；8 源真實番號健康金絲雀 smoke（三態 quorum 判讀）+ cov-floor 84% 流程地板
- 本地 WebP 縮圖快取（opt-in，SSD 出圖不碰 NAS + blur-up 燈箱）+ 燈箱單筆刪除（只刪 DB row）；VR 投影標籤保留 + 自動 VR tag
- 外部媒體管理器相容（Jellyfin/Emby/Kodi 四態：poster/fanart 命名 + cd1/cd2 合併 + NFO 補欄 + Scanner 識別外部封面）；dim 暗色主題色彩編碼修復；進階搜尋畢業為永久常駐核心

測試數 2937 → 4089。

## 0.8.x 系列 (v0.8.0 ~ v0.8.10, 2026-04-28 ~ 2026-05-28)

- Charter Pilot：Fluent 2 視覺語言全站統一（§1–§6 + ease/DURATION 三角色）+ Ghost-fly Lightbox 共用化 + Alpine 釘版四插件 + 全站通知中心
- 全站前端 ESM 模組化（Import Maps，巨型單檔全解體）+ lint toolchain（eslint flat config + stylelint，frontend_lint 905→450）
- 以圖搜圖 CLIP Beta 出貨後轉向純規則式相似度排序器（拔 ML 依賴、主 ZIP 271MB→43MB）
- Tag Alias 跨語言系統 + Search→Showcase pipeline 即時化（GhostFly + DB 即時 upsert）+ Onboarding Scanner-first 翻轉 + SSRF 白名單 + 女優查詢 json_each 重寫

測試數 2705 起（系列歷經 CLIP 上下架與 lint 瘦身，末值未單列）。

## 0.7.x 系列 (v0.7.0 ~ v0.7.8, 2026-04-10 ~ 2026-04-26)

- Agentic AI API 平台首發（batch-search / generate-from-ids / enrich-single / collection-sql / capabilities manifest）
- User Tags 三層整合（DB + NFO `<user_tag>` + API + Search/Showcase 雙頁 UI）
- Actress Favorite + Showcase 女優模式（actresses DB + Orchestrator 4 路並行 + 女優 Grid/Lightbox/Hero + GSAP）+ Actress Alias CRUD
- Scanner 一鍵補完（missing-check + batch-enrich SSE 分批）+ Ghost Fly 轉場 + WinFsp/rclone 掛載相容 + 劇照幽靈 URL 修正

測試數 1818 → 2705。

## 0.6.x 系列 (v0.6.0 ~ v0.6.7, 2026-03-29 ~ 2026-04-09)

- 四語系 i18n 建立（繁中/簡中/日文/英文，~477 key + `core/i18n.py` + `window.t()` + JavBus lang 連動）
- Agentic AI API 初版（batch-search / generate-from-ids / enrich-single / collection-sql / capabilities）+ OpenAI Compatible 翻譯 Provider
- Alpine.js 技術債清理（6 頁統一 `Alpine.data()` 註冊、移除 bridge.js/init.js、SearchCore 全域消除）
- Scanner Jellyfin 圖片手動觸發 + TTL 快取 + 前端動效補強（checkmark/shake + Load More stagger + alert→toast）

測試數 1366 → 1634。

## 0.5.x 系列 (v0.5.0 ~ v0.5.5, 2026-03-25 ~ 2026-03-28)

JavBus Scraper 完全重寫（移除 jvav 第三方依賴）+ Video Model 擴充（director/duration/label/series/sample_images）+ Sample Gallery 元件（extrafanart 全鏈路）+ Metadata Pipeline 補齊（NFO 讀寫全欄位）+ Maker 名稱對照表重建（雙層 JSON + shared loader）+ DMM 模糊搜尋 + 全來源欄位補齊（Jav321/AVSOX/HEYZO/FC2/D2Pass）+ 字幕檔自動偵測搬移 + Proxy direct 模式。測試數 1007 → 1366。

## 0.4.x 系列 (v0.4.0 ~ v0.4.4, 2026-03-08 ~ 2026-03-19)

GSAP 搜尋頁動畫系統（SSE 漸進搜尋 + Mini-Burst + Grid↔Detail 共享封面轉場 + Cover State Machine）+ GSAP Showcase 動效系統（Flip 排序 + stagger 分頁 + Motion Lab）+ Lightbox 重設計（glass circle overlay + metadata panel）+ Source Link 設定 + 安裝腳本升級 + 測試套件大整合（去重 + conftest 統一 + 覆蓋率補強）。測試數 731 → 1007。

## 0.3.x 系列 (v0.3.0 ~ v0.3.6, 2026-02-08 ~ 2026-02-20)

Bootstrap → DaisyUI + Alpine.js 全站遷移 + Showcase 動態化（SQLite SSR 取代靜態 iframe）+ Search Grid Mode + 女優資料卡（Graphis + JavBus 並行）+ Fluent Material Boost（Mica/Acrylic）+ GSAP 前置基礎建設 + Scraper 擴充（D2Pass/HEYZO/DMM/gfriends/Graphis）+ 路徑工具統一（uri_to_fs_path/pathToDisplay）+ UNC 路徑修正 + 版本標記覆蓋保護 + Jellyfin 圖片模式。測試數 523 → 817。

## 0.2.x 系列 (v0.2.0 ~ v0.2.4, 2026-01-22 ~ 2026-02-07)

Design System 建立（Fluent Design 2 視覺語言 + AV Card 4 變體 + Token 系統）+ macOS 支援（Alpha）+ AI 翻譯雙引擎（Ollama + Gemini）+ 多層目錄結構 + FC2/無碼搜尋 + SQLite 本地收藏庫 + Scraper 模組化架構（5 個獨立 scraper）+ 番號後綴清理修正。測試數 126 → 564。

## 0.1.x 系列 (v0.1.0 ~ v0.1.4, 2026-01-15 ~ 2026-01-17)

初始版本發布：多來源番號搜尋（JavBus/Jav321/JavDB）+ Gallery UI + 智慧搜尋 + 批次搜尋 + NFO 自動補全 + Settings（Dark Mode + Ollama 翻譯 + 輸出格式設定）+ 新手教學（Tutorial 4 步驟）+ Windows 打包 + 路徑工具統一（path_utils）+ 版本管理集中化。測試數 115 → 311。

> 完整歷史紀錄請見 [CHANGELOG_ARCHIVE.md](CHANGELOG_ARCHIVE.md)
