[pytest]
pythonpath = .
testpaths = tests
addopts = -v
asyncio_mode = auto
markers =
    smoke: tests that connect to external services (deselect with '-m "not smoke"')
    integration: marks tests that require external services
